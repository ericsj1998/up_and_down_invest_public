/**
 * 페이퍼넷 자금 받기 — **받는 화면으로 보낸다**.
 *
 * 🔴 **여기서 받아지지 않는다.** Gate 는 테스트 자금 지급에 API 를 열어 두지 않았고,
 * 웹 콘솔에서만 받을 수 있다. 단추가 스스로 받는 것처럼 보이면 눌러 보고 나서야 알게
 * 되므로, 문구로 **무엇을 하는 단추인지** 못 박는다 (절대 규칙 #8).
 *
 * ⚠️ **자격 판정을 걷어냈다** (2026-08-21 · 사용자 확인: *"총 자산이 1000 이하여도
 * 상관없네. 그건 다른 내용의 문서였었어."*). 이전에는  이 *"총 자산이 1000 을
 * 넘으면 못 받는다"* 로 단추를 막고 있었는데, 그 규칙은 **이 화면과 무관한 문서**에서
 * 온 것이었다. 없는 규칙으로 막으면 링크를 눌러 볼 기회조차 사라진다.
 *
 * ⛔ 1회 지급액 1000 · 72시간 주기도 **같은 출처**였으므로 같이 지운다. 근거가 무너진
 * 문서에서 온 값을 하나만 남겨 두면, 남은 쪽이 확인된 사실처럼 보인다 — 확인한 것만
 * 화면에 적는다.
 */

/**
 * 자금 화면 — **사람이 직접 열어 확인한 주소**다 (2026-08-21).
 *
 * 🔴 두 번 틀렸던 값이다. 처음에는 검색 결과의 `/testnet/futures_trade/USDT/BTC_USDT`
 * 를 확인 없이 넣어 빈 화면이 떴고, 그 반작용으로 **거래 화면이 아니라 도움말 문서**를
 * 열게 바꿨다. 그런데 그 문서가 위의 틀린 규칙들의 출처였다.
 *
 * ⚠️ 우리는 이 값을 **스스로 확인할 수 없다** — Gate 는 자동 요청을 Cloudflare 로 막아서
 * 존재가 확실한 페이지조차 403 이다. 즉 근거가 측정이 아니라 **사람의 확인**인 종류의
 * 값이고, 그래서 누가 언제 확인했는지를 여기 적어 둔다.
 */
export const FUNDS_URL = "https://testnet.gate.com/myaccount/myfunds";

/** 링크가 죽어도 이건 안 죽는다 — 확인된 주소를 글로도 적는다. */
export const PATH = "testnet.gate.com → myaccount → myfunds";

export function Faucet() {
  return (
    <span className="row" style={{ gap: "6px", alignItems: "center" }}>
      <a
        className="btn small"
        href={FUNDS_URL}
        target="_blank"
        rel="noreferrer"
        title={"자금 화면이 열린다 — " + PATH}
      >
        테스트 자금 받기
      </a>
      {/* 🔴 **경로를 글로도 적는다.** 링크는 죽지만 이 문장은 안 죽는다 — 실제로
          처음 넣었던 깊은 주소가 빈 화면이었다 (2026-08-20). */}
      <span className="card-hint">
        {PATH} · <b>API 없음</b>
      </span>
    </span>
  );
}
