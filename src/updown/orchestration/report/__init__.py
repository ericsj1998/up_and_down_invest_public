"""이메일 성과 리포트 (T35) — **조립만 한다**.

원장(`wf_trades`)과 거래소 자금 원장(`account_book`)을 **나란히** 내고, 갈리면
"갈렸다"고 쓴다 — 한쪽을 골라 보내지 않는다. 집계 계산은 원장의 기존 성질
(`gain_pct`·`realized_rr`)을 재사용하고 새 계산을 만들지 않는다.

리포트는 리스크 증가 행동이 아니다 (§1.2.1) — 실패해도 매매를 막지 않고 로그·이력만
남긴다.
"""
