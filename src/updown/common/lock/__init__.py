"""분산 락 (spec §12.5, plan D-3).

`redis-py` 의 `SET NX PX` + TTL 갱신을 직접 구현한다 — 규약 §3 의 결정이다.
`redis.asyncio` 의 내장 `Lock` 을 쓰지 않는 이유는 **락 상실 감지 훅**이 필요하기
때문이다: engine 은 갱신 실패를 알아채는 즉시 스케줄러를 멈춰야 한다 (§12.5).
"""
