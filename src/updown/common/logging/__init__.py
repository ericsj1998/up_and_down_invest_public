"""로깅 기반 — 구조화 로그·trace_id 전파·감사 적재 (spec §4.14, §1.2.1, P5).

원칙 P5 는 "모든 행위는 로그 우선"이지만, 문자 그대로 구현하면 **로그 DB 장애가 손절을
막는다.** 감사 추적을 지키려다 원금을 잃는 역전이다.

그래서 spec §1.2.1 이 행동을 리스크 방향으로 나눈다:

| 분류 | 로그 실패 시 |
|------|-------------|
| 리스크 **감소** (손절·청산·스탑 상향·주문 취소·브레이커) | **무조건 집행** + 폴백 파일 |
| 리스크 **증가** (신규 진입·추가 매수) | 보류 |
| 조회·분석 | 보류 |

**분류를 명시하지 않으면 기본값은 보류다** — 새 행동이 실수로 무단 집행되지 않게.
판정은 `policy.py`, 조립은 `audit.py` 가 한다.
"""
