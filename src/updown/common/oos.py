"""out-of-sample 봉인 경계 — **한 곳에서만 정한다**.

이 경계 뒤의 봉은 P1-8 최종 판정용으로 남겨 둔 것이다 (§1-0t T10). 측정도, 튜닝도,
**눈으로 보는 것도** 하지 않는다.

## 🔴 왜 상수 하나를 위해 모듈을 만드나

이 값이 두 곳에 적히면 한쪽만 움직이는 날이 온다. 그러면 봉인은 **깨진 줄도 모르는
채로** 깨진다 — 백테스트는 여전히 "out-of-sample" 이라고 출력하고, 그 라벨을 믿고
G1 게이트를 통과시킨다. 원래 `scripts/run_matrix.py` 에 문자열로 박혀 있었고,
점검기(`orchestration/inspection`)가 같은 값을 필요로 하면서 두 번째 사본이 생길
참이었다.

## ⛔ 눈으로 보는 것도 오염이다

봉인의 목적은 "이 구간에 맞춰 규칙을 고르지 않았다"는 보증이다. 사람이 그 구간 차트를
보고 인상을 얻으면, 이후 어떤 규칙을 후보로 올릴지가 이미 그 인상에 물든다. 코드가
그 구간을 안 읽는 것만으로는 부족하다 (절대 규칙 #11 · §5.6.2).

그래서 점검기는 이 경계 **뒤의 시점을 뽑지 않는다**.
"""

from datetime import UTC, datetime

OOS_BOUNDARY = datetime(2026, 1, 1, tzinfo=UTC)
"""이 시각 **미만** 봉만 학습·측정·열람에 쓴다 (§1-0t T10).

⚠️ 이 값을 옮기려면 그때까지의 모든 판정이 무효가 된다 — 경계를 옮긴다는 것은
"봉인해 둔 구간을 이미 봤다"는 뜻이기 때문이다. 옮길 일이 있으면 축 후보가 아니라
스펙 개정 안건이다.
"""
