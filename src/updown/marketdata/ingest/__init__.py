"""캔들 수집 파이프라인 (P0-8 · spec §4.2, §12.1, §7).

| 모듈 | 책임 | 순수성 |
|------|------|--------|
| `timeframes` | 봉 간격·경계·기대 봉 수 | **순수** (원칙 P1) |
| `integrity` | OHLC·결측·스파이크 검사 → 위반 구간 | **순수** (원칙 P1) |
| `repository` | candles upsert · 커버리지 · 이슈 적재 | I/O |
| `backfill` | 창 단위 과거 수집 · 재개 | I/O 조립 |
| `collector` | 주기 수집 (봉마감 + 갭 메움) | **P0-8-6 — P0-9 스케줄러 선행** |

**Phase 0 은 코인 전용이다.** 24시간 장(spec §7)이라 "기대 봉 수 = 구간/간격"이 정확히
성립하고, 그 산수가 재개 판정과 결측 검사의 근거다. 주식(P3)에는 장 운영시간 캘린더가
필요하며 없이 쓰면 매일 밤이 결측으로 잡힌다.

수정주가·기업행동(spec §12.1)은 **주식 대상**이라 P3-11 이다 — Phase 0 범위 밖이다.
"""
