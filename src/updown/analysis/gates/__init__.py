"""셋업 승격 게이트 (spec §5.4).

§5.4 는 셋업이 제안으로 **승격되기 전에** 통과해야 하는 게이트 넷을 정한다:

| # | 게이트 | 여기 있는가 | 왜 |
|---|---|---|---|
| 1 | 청산 > 진입 | ❌ | 활성 청산 신호는 포지션 상태이며 `portfolio`/`decision` 이 안다 |
| 2 | **추세 (역추세 차단)** | ✅ `trend_gate` | 입력이 `analysis.trend` 의 `TrendState` 다 |
| 3 | 시장 국면 (Regime) | ❌ | Regime 은 `decision` 소관이다 (CLAUDE.md 레이어 표) |
| 4 | 합류 점수로 선택 | ❌ | 기각이 아니라 **선택**이라 성격이 다르다 (§5.5) |

**2번만 여기 있는 이유는 입력의 소속이다.** 게이트를 한곳에 모으려고 Regime 을 끌어오면
`analysis → decision` 역방향 import 가 되어 계층 계약이 깨진다. 게이트는 각자 입력이 사는
계층에 두고, 조립은 `orchestration` 이 한다.
"""
