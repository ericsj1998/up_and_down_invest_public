"""공용 지표 — **자체 구현** (P1-2 · spec §6.1, §2.1, 절대 규칙 #9).

라이브러리에 위임하지 않는다. 이유는 편의가 아니라 **재현성**이다 — 라이브러리 버전이
올라가며 계산이 미세하게 바뀌면 `rule_id@version` 단위 성과 귀속(§4.14)과 백테스트
재현(원칙 P1)이 근거를 잃는다. pandas-ta 는 **테스트 정답지 대조용**이며 런타임
의존성이 아니다.

| 모듈 | 책임 |
|------|------|
| `series` | **계산 계약** — 입력 검증, 워밍업 규칙, Wilder 평활 |
| `ma` | SMA/EMA 20·60·120·200, 정배열/역배열, 골든/데드크로스 |
| `atr` | True Range, ATR(14) — 손절폭 산정 표준 |
| `rsi` | RSI(14), 다이버전스 |
| `volume` | 거래량 배수 (직전 20봉 평균 대비) |
| `snapshot` | 시리즈 일괄 계산 + `Indicators` 조립 |

**계약 세 줄** (`series` 모듈 docstring 참조):

1. 출력 길이 == 입력 길이. 워밍업은 `None` 이고 **잘라내지 않는다**
2. `None` 은 "아직 판단할 수 없음"이다. 0 이 아니다
3. 가격 단위 지표는 `Decimal`, 무차원 지표는 `float`

1번이 안전장치다 — `structures/` 가 봉 번호를 x축으로 쓰므로, 지표가 워밍업을 잘라
반환하면 손절가가 엉뚱한 봉의 ATR 로 계산되면서 **아무 예외도 나지 않는다.**
"""
