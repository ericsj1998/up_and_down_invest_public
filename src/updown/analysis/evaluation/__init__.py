"""룰 품질 측정 — **사람 눈 라벨링의 대체물** (spec §5.6.6).

`정탐/오탐`을 사람이 눈으로 매기는 것을 금지하는 대신, 같은 질문을 **규칙으로** 답한다:
"탐지 후 실제로 규칙대로 움직였는가."

⛔ **`analysis/detectors/` 는 이 패키지를 import 하지 않는다.** 여기 있는 함수들은
**의도적으로 미래 봉을 본다** — 그것이 측정의 목적이다. 탐지기가 이걸 부르면 그 순간
미래 참조가 되고, `AsOfSequence`(P1-3)가 막아온 것이 정문으로 들어온다.
`.importlinter` 의 `forbidden` 계약이 이 방향을 차단한다.
"""
