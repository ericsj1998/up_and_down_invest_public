"""구조물 공용 모듈 (P1-1 · spec §4.3.1 "공용 유틸 계층 분리").

셋업 플러그인이 **공유하는** 재료를 여기서만 계산한다. 플러그인마다 스윙을 따로
계산하면 합류(confluence) 판정이 불가능해지고, 추세선을 따로 그으면 "꼬리 끝 기준"
전역 규칙(spec §6.5)을 어기는 경로가 생긴다.

| 모듈 | 책임 |
|------|------|
| `params` | 표준 이론값 주입 (spec §5.6.1 ② — 미세조정하지 않는다) |
| `swing` | 스윙 하이/로우 — **꼬리 끝 좌표의 출발점** |
| `trendline` | 추세선·평행 채널 |
| `box` | 수평 지지·저항 레벨 |
| `confluence` | 중첩 정량화 — 점수는 **종류 수**다 (spec §5.5) |
| `repository` | 영속화 + 생애주기 전이 (**삭제 없음**) |

`swing`·`trendline`·`box`·`confluence` 는 전부 순수 함수다. DB·현재시각·난수를 보지
않으므로 골든 스냅샷으로 회귀를 고정할 수 있다 (원칙 P1).
"""
